// Entry point — kept at the project root so `node server.js` / `nodemon
// server.js` work the way most backend projects expect. The actual app
// (config, models, routes, middleware) lives under src/ for organization.
import "./src/server.js";
